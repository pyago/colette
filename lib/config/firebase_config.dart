/// Flip to true after running `flutterfire configure` and filling
/// [DefaultFirebaseOptions] in `lib/firebase_options.dart`.
class FirebaseConfig {
  /// Set true once a real Firebase web app is configured.
  static const enabled = false;
}
